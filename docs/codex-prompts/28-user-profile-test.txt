현재 작업 브랜치는 test/28-user-profile-api 이다.
이 작업은 feat/27-user-profile-api 가 반영된 상태를 기준으로 한다.
직전 브랜치의 controller / service / dto, AGENTS.md, docs/user-part-spec.md를 먼저 읽고 테스트를 구성해라.

[작업 전 필수]
1. 기존 테스트 코드가 있다면 먼저 찾아서 스타일을 요약해라.
2. UserController와 UserService의 최종 코드를 다시 읽어라.
3. snake_case 직렬화 기대와 ApiResponse 구조를 다시 점검해라.
4. 현재 프로젝트에서 controller 테스트가 어떤 방식으로 작성되는지 먼저 확인해라.

[이번 브랜치 목표]
현재 사용자 정보 조회 API가 기존 프로젝트 규칙에 맞게 동작하는지 검증하고 누락을 정리한다.

[이번 브랜치에서 구현할 범위]
- GET /users/me 테스트 추가
- 가능하면 MockMvc 기반 검증
- ApiResponse 구조 검증
- snake_case 필드 검증
- X-USER-ID 헤더 전달 케이스 검증
- 누락 import / wiring / 직렬화 리스크 점검

[구현 원칙]
- 테스트 인프라를 과하게 확장하지 마라.
- 현재 프로젝트 실행 환경 제약을 감안해서 가장 현실적인 방식으로 검증하라.
- 한국어 톤과 기존 응답 규칙을 유지하라.
- 실행이 어렵다면 최소한 테스트 코드 초안과 수동 검증 시나리오를 함께 제시하라.

[출력 형식]
1. 참고한 기존 테스트 / 컨트롤러 파일
2. 생성 / 수정할 파일 목록
3. 각 파일의 전체 코드 또는 테스트 시나리오
4. 발견한 리스크 / 추가 TODO
5. 최종 인수인계 메모

작업 종료 전에 반드시 아래를 출력해라.
1. 이번 브랜치에서 생성한 파일
2. 이번 브랜치에서 수정한 파일
3. 다음 작업자가 먼저 읽어야 할 파일 목록
4. 남은 TODO
